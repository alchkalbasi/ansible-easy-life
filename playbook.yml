- hosts: rustfs-arvan
  gather_facts: true
  become: true
  roles:
    - rustFS
  tags:
    - rustfs

- hosts: elasticsearch-arvan
  gather_facts: true
  become: true
  roles:
    - roles/elk/elasticsearch
  tags:
    - elasticsearch