NETWORK1="{{ docker_networks[0] }}"
NETWORK2="{{ docker_networks[1] }}"
TRAEFIK_IMAGE="{{ traefik_docker_image }}"
RESTART_POLICY="{{ traefik_restart_policy }}"
HOSTNAME="{{ traefik_hostname }}"
CONTAINER_NAME="{{ traefik_container_name }}"
TRAEFIK_LOG_LEVEL="{{ traefik_log_level }}"
TRAEFIK_API_DASHBOARD="{{ traefik_api_dashboard }}"
TRAEFIK_API_INSECURE="{{ traefik_api_insecure  }}"
TRAEFIK_DOCKER_PROVIDER="{{ traefik_docker_provider }}"
TRAEFIK_DOCKER_NETWORK="{{ traefik_docker_network }}"
TRAEFIK_DOCKER_EXPOSE_DEFAULT="{{ traefik_docker_expose_default }}"
TRAEFIK_HTTP_REDIRECT_TO="{{ traefik_http_redirect_to }}"
TRAEFIK_CERT_RESOLVER_LE_ACME_TLSCHALLENGE="{{ traefik_cert_resolver_le_acme_tlschallenge }}"
TRAEFIK_CERT_PATH="{{ traefik_cert_path }}"
TRAEFIK_ACME_EMAIL="{{ traefik_acme_email }}"
TRAEFIK_WEB_AUTH_USER="{{ traefik_web_auth_user }}"
TRAEFIK_WEB_AUTH_PASS="{{ traefik_web_auth_pass }}"
TRAEFIK_DOMAIN="{{ traefik_domain }}"
TRAEFIK_TLS="{{ traefik_tls }}"
TRAEFIK_CERTRESOLVER="{{ traefik_certresolver }}"
TRAEFIK_LOADBALANCER_PORT="{{ traefik_loadbalancer_port }}"