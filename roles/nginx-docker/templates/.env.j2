NETWORK1="{{ docker_networks[0] }}"
NETWORK2="{{ docker_networks[1] }}"
NGINX_IMAGE="{{ nginx_docker_image }}"
RESTART_POLICY="{{ nginx_restart_policy }}"
HOSTNAME="{{ nginx_hostname }}"
CONTAINER_NAME="{{ nginx_container_name }}"
WEBROOT_PATH="{{ nginx_webroot_path }}"
NGINX_FULL_DOMAIN="{{ subdomain }}.{{ domain }}"