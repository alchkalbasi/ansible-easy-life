#SPDX-License-Identifier: MIT-0
---
# handlers file for rustFS

- name: Restart Nginx
  community.docker.docker_compose_v2:
    project_src: "{{ service_dir }}"
    services:
      - nginx
    state: restarted
  listen: "reload nginx"
  tags:
    - restart-nginx